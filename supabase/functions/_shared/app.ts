// Shared application-wide constants, used by multiple edge functions.
// NOTE: the client has its own copy in src/config/app.ts (APP_CONFIG.appName).
// Keep the two in sync — they are separate runtimes and cannot share one file.
export const APP_NAME = "Learn Ivrit";
