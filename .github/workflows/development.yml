name: Development

on:
  push:
    branches: [development]
  pull_request:
    branches: [development]
  workflow_call:
    inputs:
      branch_name:
        required: true
        type: string

permissions:
  contents: write
jobs:
  set-branch-name:
    runs-on: ubuntu-latest
    steps:
      - name: set branch-name
        run:
          branch_name=${branch_name:=${{ inputs.branch_name }} || ${{ github.head_ref || github.ref_name }} }

  display-branch-inside-development0:
    runs-on: ubuntu-latest
    steps:
      - name: Display current branch name inside development at startup
        run:
          echo "Running inside of development.yml"
          echo "The current branch name is ${{ github.ref }}"
          echo "The current branch name is ${{ github.ref_name }}"
          echo "The current branch name is ${{ inputs.branch_name }}"
          echo "The current branch name is ${{ branch_name }}"

  base-checks:
    name: Base Checks
    uses: ./.github/workflows/base-checks.yml
    with:
      branch_name: ${{ github.head_ref || github.ref_name }}

#  unit-tests:
#    name: Unit Tests
#    runs-on: ubuntu-latest
#    needs: base-checks
#    environment: development
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: '20'
#          cache: 'npm'
#
#      - name: Install dependencies
#        run: npm ci
#
#      - name: Run unit tests
#        run: npm run test:unit

  display-branch-inside-development:
    runs-on: ubuntu-latest
    steps:
      - name: Display current branch name inside development
        run:
          echo "Running inside of development.yml"
          echo "The current branch name is ${{ github.ref }}"
          echo "The current branch name is ${{ github.ref_name }}"
          echo "The current branch name is ${{ inputs.branch_name }}"
          echo "The current branch name is ${{ branch_name }}"

  promote-to-integration:
    name: Promote to Integration
    runs-on: ubuntu-latest
    needs: [base-checks]
#    needs: [base-checks, unit-tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/development'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Merge to integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin integration:integration
          git checkout integration
          git merge --no-ff origin/development -m "Auto-promote from development to integration"
          git push origin integration
