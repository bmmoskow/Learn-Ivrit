name: Integration

on:
  push:
    branches: [development, integration]
  workflow_call:
    inputs:
      branch_name:
        required: true
        type: string

permissions:
  contents: write
jobs:

  display-branch-inside-intgration0:
    runs-on: ubuntu-latest
    steps:
      - name: Display current branch name inside integration
        run:
          echo "Running inside of integration.yml - part 0"
          echo "The current branch name is ${{ github.ref }}"
          echo "The current branch name is ${{ github.ref_name }}"
          echo "The current branch name is ${{ inputs.branch_name }}"

  development-checks:
    name: Development Checks
    uses: ./.github/workflows/development.yml
    with:
      ref: ${{ inputs.branch_name }}

#  integration-tests:
#    name: Integration Tests
#    runs-on: ubuntu-latest
#    needs: development-checks
#    environment: test
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: '20'
#          cache: 'npm'
#
#      - name: Install dependencies
#        run: npm ci
#
#      - name: Run integration tests
#        run: npm run test:integration
#        env:
#          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
#          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
#          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
  display-branch-inside-intgration:
    runs-on: ubuntu-latest
    steps:
      - name: Display current branch name inside integration
        run:
          echo "Running inside of integration.yml"
          echo "The current branch name is ${{ github.ref }}"
          echo "The current branch name is ${{ github.ref_name }}"
          echo "The current branch name is ${{ inputs.branch_name }}"

  promote-to-ui:
    name: Promote to UI
    runs-on: ubuntu-latest
    needs: [development-checks]
#    needs: [development-checks, integration-tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/integration'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Merge to ui
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin ui:ui
          git checkout ui
          git merge --no-ff origin/integration -m "Auto-promote from integration to ui"
          git push origin ui
